var React = require('react');
var ReactDOM = require('react-dom');
var ElevatorBox = require(__dirname + '/components/elevator_box.jsx');

ReactDOM.render(
  <ElevatorBox />,
  document.getElementById('content')
);
